import 'package:flutter/material.dart';

/// The color palette for the App UI Kit.
abstract class AppColors {
  /// The primary black color.
  static const Color primaryBlack = Color.fromRGBO(0, 0, 0, 1);

  /// The secondary black color.
  static const Color secondaryBlack = Color.fromRGBO(121, 121, 121, 1);

  /// The white 60% color.
  static const Color whiteBorder = Color.fromRGBO(233, 233, 233, 1);

  /// The primary white color.
  static const Color primaryWhite = Color.fromRGBO(255, 255, 255, 1);

  /// The primary orange color.
  static const Color primaryGreen = Color.fromRGBO(0, 132, 101, 1);

  /// The primary orange light color.
  static const Color primaryOrange = Color.fromRGBO(239, 160, 95, 1);

  /// The primary orange light color.
  static const Color primaryOrangeLight = Color.fromRGBO(255, 143, 80, 0.2);

  /// The background primary color.
  static const Color backgroundPrimary = Color.fromRGBO(255, 255, 255, 1);

  /// The background post color.
  static const Color backgroundPost = Color.fromRGBO(255, 255, 255, 1);

  /// The menu color.
  static const Color menu = Color.fromRGBO(243, 244, 244, 1);

  /// The black 60% color.
  static const Color black60 = Color.fromRGBO(0, 0, 0, 0.6);

  /// The black 38% color.
  static const Color black38 = Color.fromRGBO(0, 0, 0, 0.38);

  /// The black 12% color.
  static const Color black12 = Color.fromRGBO(0, 0, 0, 0.12);

  /// The black 8% color.
  static const Color black8 = Color.fromRGBO(0, 0, 0, 0.08);

  /// The black 4% color.
  static const Color black4 = Color.fromRGBO(0, 0, 0, 0.04);

  /// The black 20% color.
  static const Color black20 = Color.fromRGBO(0, 0, 0, 0.2);

  /// The black 25% color.
  static const Color black25 = Color.fromRGBO(0, 0, 0, 0.25);

  /// The white 90% color.
  static const Color white90 = Color.fromRGBO(255, 255, 255, 0.9);

  /// The white 60% color.
  static const Color white60 = Color.fromRGBO(255, 255, 255, 0.6);

  /// The white 30% color.
  static const Color white30 = Color.fromRGBO(255, 255, 255, 0.30);

  /// The white 38% color.
  static const Color white38 = Color.fromRGBO(255, 255, 255, 0.38);

  /// The default button color.
  static const Color button = Color.fromRGBO(109, 108, 108, 1);

  /// The default button disabled color.
  static const Color buttonDisabled = Color.fromRGBO(62, 62, 62, 1);

  /// The white 12% color.
  static const Color white12 = Color.fromRGBO(255, 255, 255, 0.12);

  /// The white 8% color.
  static const Color white8 = Color.fromRGBO(255, 255, 255, 0.08);

  /// The white 4% color.
  static const Color white4 = Color.fromRGBO(255, 255, 255, 0.04);

  /// The blue color.
  static const Color blue = Color.fromRGBO(65, 146, 239, 1);

  /// The red color.
  static const Color red = Color(0xFFFA3737);

  /// The shadow color.
  static const Color shadow = Color.fromRGBO(235, 235, 235, 1);

  /// The text background color.
  static const Color textBackground = Color.fromRGBO(118, 118, 118, .32);

  /// The grey color.
  static const Color grey = Color(0xFF1F1F1F);

  /// The light grey color.
  static const Color lightGrey = Color(0xFFF3F4F4);

  /// The green color.
  static const Color green = Color(0xFF5DD525);
}
